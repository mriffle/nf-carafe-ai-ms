name: Stub Tests

on:
  push:
    branches: ['**']
  pull_request:
    branches: [main]

jobs:
  stub-tests:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        nextflow-version: ['24.10.4']
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Java
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'

      - name: Install Nextflow
        uses: nf-core/setup-nextflow@v2
        with:
          version: ${{ matrix.nextflow-version }}

      - name: Run stub tests
        run: bash tests/run_stub_tests.sh
